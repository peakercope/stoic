# Kanban Board

A Trello-style sprint board — columns, drag-and-drop, search, filters, and live
statistics.

```bash
# once, from the repository root: the Vite alias resolves to dist/prod
yarn install && yarn build

# then, here
yarn install
yarn dev
```
