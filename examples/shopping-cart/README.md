# Shopping Cart

A realistic e-commerce cart — products, quantities, coupons, shipping options, tax
and a live order total. This is the flagship Stoic example.

```bash
# once, from the repository root: the Vite alias resolves to dist/prod
yarn install && yarn build

# then, here
yarn install
yarn dev
```
